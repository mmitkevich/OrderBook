{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "echo",
            "type": "shell",
            "command": "clang++ -g -O0 -std=c++14 -o test1 test1.cpp",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}